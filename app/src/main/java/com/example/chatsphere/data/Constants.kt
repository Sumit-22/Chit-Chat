package com.example.chatsphere.data

const val USER_NODE = " User" // here we don't directly just write insert at User just like in we did in shopify app, here we crete a node ex:- we don't use "db.collection("users") " line 53 at LCViewModel
const val CHATS = " Chats"
const val MESSAGE = "message"
const val STATUS = "status"